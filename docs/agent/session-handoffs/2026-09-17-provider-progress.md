# Provider progress and cancellation

- Goal: diagnose the user's first-line Codex stall and make provider progress/cancellation useful without consuming generation usage.
- Inspected: agent guide/state/map/questions; provider service, Codex/Claude/RPC/MCP adapters, assistant UI/domain/tests, next-service and Python worker; read-only summary of the user's failed AI record and non-secret local reasoning setting; official App Server documentation and locally generated schema.
- Changed: `electron/provider-{service,codex,claude,rpc,context}.cjs`, `electron/tests/providers.test.cjs`, `src/components/AssistantPanel.tsx`, `src/domain/ai.ts`, `src/domain/ai.test.ts`, `tests/providers-{harness.html,harness.tsx,progress.spec.ts}`, `docs/design/providers.md`.
- Commands: `node --test electron/tests/providers.test.cjs` (54 assertions pass); `npx vitest run src/domain/ai.test.ts` (3 pass); `npx playwright test tests/providers-progress.spec.ts` (1 simulated-provider UI pass); targeted Prettier; `npm run typecheck` during concurrent integration; read-only `get_source_context` for real first Araújo passage; local Codex authentication/model-list check only; old request history validation in a temporary copy.
- Worked: actual first-line MCP 388 ms; Codex auth/catalog 735 ms, medium supported/default; legacy history readable and original bytes unchanged. Durable stage/timestamp history, inactive-phase/total deadlines, immediate cancellation during unresolved context, MCP and CLI cancellation, graceful turn interruption, no late generation/output after cancellation, model reasoning activity, completed-item fallback, saved explicit effort and local UI history reconciliation are tested. No generation request was sent.
- Failed/uncertain: the old record proves completed context, zero output and a 180-second timeout, but lacks the stage evidence needed to explain the exact upstream stall. Inherited global `ultra` reasoning is a plausible contributor, not a confirmed provider diagnosis. Initial typecheck was temporarily blocked by the other agent's not-yet-created RuntimeTree component; root must run the integrated check after all files land. One manual MCP probe used the wrong short source alias, then the actual `araujo_catecismo_1686` identity succeeded.
- Remaining questions: observe the user's next intentional generation using the new phase history; do not spend usage on a routine generation regression.
- Suggested next prompt: “Inspect the saved provider phase history for my latest request and diagnose the exact delay without sending another generation.”
